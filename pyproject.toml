[project]
name = "vulnguard"
version = "0.3.0"
description = "OWASP-focused lightweight static security scanner"
readme = "README.md"
requires-python = ">=3.8"

dependencies = [
    "typer",
    "pyyaml",
    "openai"
]

[project.scripts]
vulnguard = "vulnguard.cli:app"

